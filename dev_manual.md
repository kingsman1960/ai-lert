# 🛠️ 개발 가이드: 하일브론 리스크 가이드 (Implementation Hints)

이 문서는 프로젝트 구현을 위한 데이터 소스와 기술적 방향성을 제공합니다. 핵심 비즈니스 로직과 상세 설정은 각 개발자의 구현 영역으로 남겨두어, 각자의 설계 방식에 맞게 구축하시기 바랍니다.

## 📡 1. 데이터 소스 및 API 가이드

### A. 실시간 위험 분석 (Backend)
사용자의 위치를 기반으로 다음 데이터들을 취합하여 리스크 스코어를 산출하십시오.
- **NINA API**: 지역별 재난 경보 및 공식 경고문 취합.
- **PEGELONLINE API**: 인근 수위 관측소(Gauge)의 실시간 수위 데이터 및 임계값 분석.
- **DWD API**: 기상 특보 및 강수 예측 데이터 연동.

### B. 공간 정보 시각화 (WMS Layers)
지도 위에 레이어를 겹쳐 표시하기 위해 다음 WMS 서비스들을 활용하십시오. (상세 URL 및 레이어 이름은 공식 포털에서 확인 가능합니다.)

| 분류 | 제공 기관 | 주요 데이터 (힌트) |
| :--- | :--- | :--- |
| **하천 홍수** | LUBW | 100년 빈도 홍수 구역(ÜSG), 극한 홍수 심도 |
| **집중 호우** | LUBW | SRRM 기반 침수 예상 지역 및 침수 깊이 |
| **지질 위험** | LGRB | IGHK50/GeoLa 기반 산사태, 낙석, 싱크홀, 지반 침하 구역 |
| **기반 시설** | 연방 수로청 | Federal Waterway Network (하천망) |

## ⚙️ 2. 기술 스택 및 구현 방향

### Backend (API Server)
- **기능**: 입력된 위치(`query` 또는 `lat/lng`) → 데이터 수집 → 위험도 계산 $\rightarrow$ 가이드라인 생성.
- **추천 구조**: 여러 외부 API 호출을 효율적으로 처리하기 위한 Aggregator 패턴 적용.
- **핵심 과제**: 각기 다른 데이터 소스(경보, 수위, 지질)를 어떻게 하나의 '위험 점수'로 수치화할 것인가에 대한 로직 설계.

### Frontend (Interactive Map)
- **지도 라이브러리**: `React-Leaflet` 또는 `Leaflet` 추천.
- **레이어 제어**: 
    - `WMSTileLayer`를 사용하여 위에서 언급한 기관들의 데이터를 오버레이하십시오.
    - 사용자가 원하는 위험 정보만 선택해서 볼 수 있도록 **Layer Toggle** 기능을 구현하십시오.
- **시각적 요소**:
    - 사용자 현재 위치 및 인근 수위 관측소 마커 표시.
    - 위험 구역(Polygon) 및 추천 대피 경로(Polyline) 렌더링.
    - 주변 안전 시설물(Safe Places) 표시.

## 🚀 3. 구현 시 고려사항 (Challenge)
단순한 데이터 표시를 넘어 다음의 고도화 작업을 고민해 보시기 바랍니다.
1. **데이터 정규화**: 서로 다른 기관의 WMS 버전(`1.1.1` vs `1.3.0`)과 좌표계를 어떻게 통일하여 렌더링할 것인가?
2. **UI/UX**: 수많은 위험 레이어가 겹쳤을 때 가독성을 높이기 위한 투명도(Opacity) 및 색상 전략은 무엇인가?
3. **성능**: 여러 개의 WMS 요청이 지도 렌더링 성능에 미치는 영향을 어떻게 최적화할 것인가?
